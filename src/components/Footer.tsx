export function Footer() {
  return (
    <footer className="text-center py-4 text-xs text-gray-400 border-t border-gray-100">
      Car Cost Compare &mdash; ACT defaults. Calculations are estimates only.
    </footer>
  );
}
